version: '3'
services:
  membershipwallet-sales-site:
    container_name: membershipwallet-sales-site
    build:
      dockerfile: Dockerfile
      context: .
      target: runner
    ports:
      - "80:80"
      - "443:443"